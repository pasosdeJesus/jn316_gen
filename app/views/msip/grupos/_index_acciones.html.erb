<% if can?(:edit, Msip::Grupo) %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
    edit_modelo_path(registro),
    "data-turbo": false,
    :class => 'btn btn-sm'
  %>
<% end %>
<% if can?(:destroy, Msip::Grupo) %>
  <%= link_to 'Eliminar de base',
    msip.admin_grupo_path(registro),
    :method => :delete,
    :data => { 
      :confirm => t('.confirm', 
                    :default => t("helpers.links.confirm", 
                                  :default => '¿EStá seguro?')) 
    },
    :class => 'btn btn-sm btn-danger' %>
  <% if registro.gidNumber %>
    <%= link_to 'Eliminar de base y LDAP',
      jn316_gen.grupoldap_path(registro),
      :method => :delete,
      :data => { 
        :confirm => t('.confirm', 
                      :default => t("helpers.links.confirm", 
                                    :default => 'Are you sure?')) 
      },
      :class => 'btn btn-sm btn-danger' %>
  <% end %>

<% end %>
